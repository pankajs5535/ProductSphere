version: '3.8'

services:
  productsphere.api:
    container_name: productsphere-api

    build:
      context: .
      dockerfile: ProductSphere.API/Dockerfile

    ports:
      - "8080:8080"

    environment:
      ASPNETCORE_ENVIRONMENT: Development

    restart: unless-stopped